// Boot entry point placeholder.
// The engine bootstrap lands in a later task; this file intentionally
// contains no runtime code so the scaffold stays free of starter demo logic.
import './style.css';

export {};
